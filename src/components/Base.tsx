import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
`;

const Base = () => {
    return (
        <Wrapper>

        </Wrapper>
    )
};

export default Base;